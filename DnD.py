import requests
from Dnd_Structures import *
from Dnd_Functions import *

quick_search()
#request = input("Request: ")
#while request != "Bye.":
#	r = requests.get('https://www.dnd5eapi.co/api/' + request)
	#new_object = add_object(r.json())
#	if not class_race_check(request, r.json()):
#		print(r.text)
#	request = input("Another: ")